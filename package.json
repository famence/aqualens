{
  "name": "aqualens",
  "version": "1.0.0",
  "private": true,
  "description": "Ultimate Liquid Glass effect for the web",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/core && npm run build --workspace=packages/react",
    "build:core": "npm run build --workspace=packages/core",
    "build:react": "npm run build --workspace=packages/react",
    "dev": "npm run dev --workspace=packages/core & npm run dev --workspace=packages/react",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "changeset": "changeset",
    "publish:version": "changeset version",
    "publish:publish": "changeset publish",
    "publish": "npm run publish:version && npm run build && npm run publish:publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/famence/aqualens.git"
  },
  "license": "MIT",
  "dependencies": {
    "@aqualens/core": "^1.0.0"
  }
}
