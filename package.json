{
  "name": "aqualens",
  "version": "1.0.0",
  "private": true,
  "description": "Ultimate Liquid Glass effect for the web",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/core && npm run build --workspace=packages/react",
    "build:core": "npm run build --workspace=packages/core",
    "build:react": "npm run build --workspace=packages/react",
    "dev": "npm run dev --workspace=packages/core & npm run dev --workspace=packages/react",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@aqualens/core": "^1.0.0"
  }
}
